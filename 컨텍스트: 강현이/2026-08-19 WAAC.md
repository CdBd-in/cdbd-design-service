# 2026-08-19 WAAC 26FALL — 메이크뷰 이관 · Figma 16프레임 빌드 · CdBd 상품링크 35개

## 요약
WAAC 26FALL 룩북을 **Figma에서 8룩 × 2프레임(화보 + 상품 누끼) = 16프레임**으로 제작하고,
CdBd 에디터 **5721**의 8페이지 상품 레이어 **35개에 콜롱몰 구매 링크**를 연결·게시 반영 완료.
(사전 작업으로 기존 메이크뷰 26SUMMER 콘텐츠 구조를 역분석 — 이관 패턴 확보)

- Figma: https://www.figma.com/design/JDpEVDjkNkQNBnnP2YRcHT/WAAC_2608 (fileKey `JDpEVDjkNkQNBnnP2YRcHT`, page `0:1`)
- CdBd 에디터: https://www.cdbd.in/editor/5721
- **게시 URL: https://www.cdbd.in/mustard73/26FALL** (site_key `mustard73` / url_key `26FALL`)
- CdBd 계정: `mustard73@emka.group` / user_id `30331d7a-2e17-4e9e-b76a-d881c510ed97`
  (⚠️ 기존 문서의 `account@emka.group` / `523daa95-…` 와 **다른 계정**)
- 소스: `/Users/mustard/Desktop/mustard/CDBD/8월/2608_WAAC/`
- 참고 영상: `~/Desktop/WAAC_swp.mov` (메이크뷰 스와이프 동작) · `~/Desktop/레이어 추가_url 설정.mov` (CdBd 레이어 URL 입력 UI)

---

## A. 메이크뷰 원본(26SUMMER) 역분석 — 이관 패턴

> 이번 26FALL 작업의 직접 소스는 아니지만, "메이크뷰 → CdBd" 구조 매핑의 레퍼런스로 확보.

- 아카이브 `https://waac.makevu.me/` → 콘텐츠 1건 `https://waac.makevu.me/36da446dd3` (WAAC / 26SUMMER / 2026-04-28)
- 규격 **640×1040** (1:1.625) · **10P** · documentId `40c6f4290d`

**페이지 구조** (P1~P9 동일 패턴 / P10 아웃트로)
- BG 화보(jpg) + **swipe set 2컷** = ①투명 오버레이 + 하단 `MORE ▲` 버튼 ②상품 누끼 그리드(카테고리+품번+🛍 아이콘)
- 화보 위 **투명 클릭 핫스팟**(64×64, text="") → `event.click = openWeb` 콜롱몰 상품 URL
- P10 = WAAC 로고 + 카피 + 블랙 버튼 2 + KOLON MALL/INSTAGRAM 아이콘 + **메이크뷰 푸터(이관 시 제거 대상)**
- 상품 링크 20개 / 고유 품번 18개 (P2·P7 상품 슬라이드 완전 동일)

**자산 추출 방법 (재현용)**
1. 뷰어 HTML의 `'documentData' : {...}` 를 `json.JSONDecoder().raw_decode()` 로 파싱 → 전 페이지 레이어/좌표/링크 확보
2. S3 경로 패턴은 `viewer.min.js` 안의 템플릿 문자열에 있음:
   `{s3root}/documents/{documentId}/{deployEnv}/assets/{type}/{filename}`
   실제값 → `https://s3-ap-northeast-1.amazonaws.com/makevu-vutool/documents/40c6f4290d/production/assets/image/{fileName}`
   (`deployEnv` = `production`, `type` = `image` — `images`/`file` 등 다른 값은 전부 403)
3. 다운로드 결과 29개 → `~/Desktop/CdBd/제작서비스 2026/08/WAAC/메이크뷰 원본/` (+ `_documentData.json`)

**CdBd 등가 매핑**: 화보/그리드 2컷 = 갤러리(넘겨보기) 또는 이미지카드 2장 · 핫스팟 = `block.layers[]` + `linkButton`

---

## B. Figma 빌드 — 8룩 × 2프레임

### 소스 데이터
| 항목 | 경로 |
|---|---|
| 화보 | `리소스/{1~8}.jpg` (5000~8000px 원본) |
| 상품 누끼 원본 | `리소스/{1~8}/{품번}_LM1.webp` (610×915, **`#EAEAEA` 단색 배경**) |
| 상품명·품번·URL | `문서/E-카달로그.pptx` slide1~8 = look1~8 |

### 누끼 처리 (⚠️ Figma AI 불필요)
리소스가 **단색 `#EAEAEA` 배경**이라 ML/Figma AI 없이 결정론적으로 처리:
1. `PIL` → numpy, `bg = a[0,0]`, tol 12로 배경 후보 마스크
2. **테두리에서 BFS 플러드필** (전역 색상 임계값 ❌ — 흰 양말·흰 셔츠가 같이 지워짐)
3. alpha 합성 → `getbbox()` → `crop()` → PNG
- 결과 품질 사용자 승인 완료. 흰 상품(양말·셔츠·장갑)도 경계 깨끗함.
- 산출물: `/tmp/waac_nuki/{품번}.png`, `/tmp/waac_nuki2/*` (look2)

### 프레임 배치 (page 0:1, 전부 y=2162, x 간격 444)
| x | 프레임 | 노드 | x | 프레임 | 노드 |
|---|---|---|---|---|---|
| 0 | `01` | 1:50 | 444 | `01-swp` | 1:593 |
| 888 | `02` | 5:2 | 1332 | `02-swp` | 5:4 |
| 1776 | `03` | 9:2 | 2220 | `03-swp` | 9:14 |
| 2664 | `04` | 9:4 | 3108 | `04-swp` | 9:54 |
| 3552 | `05` | 9:6 | 3996 | `05-swp` | 9:95 |
| 4440 | `06` | 9:8 | 4884 | `06-swp` | 9:136 |
| 5328 | `07` | 9:10 | 5772 | `07-swp` | 9:218 |
| 6216 | `08` | 9:12 | 6660 | `08-swp` | 9:177 |

`01` / `01-swp` 는 사용자가 직접 제작한 기준 페이지. `02`~`08` 은 이번에 생성.

### 화보 프레임 규칙
- 380×580 프레임 · `clipsContent = true` · **자식 RECTANGLE**(frame fill ❌ — 비크롭 원칙)
- 원본 비율 그대로 **세로 fit(h=580)** → `w = 580 × (원본 w/h)`, `x = (380 − w)/2` 중앙
- 업로드 전 **3배 해상도(height 1740px)로 리사이즈** (원본 8000px 그대로면 10MB 업로드 한도 초과)

| look | 원본 | 비율 | rect w | 비고 |
|---|---|---|---|---|
| 2 | 5464×8192 | 0.667 | 386.9 | |
| 3 | 4141×5798 | 0.714 | 414.3 | 풀샷 |
| 4 | 4360×5732 | 0.761 | 441.3 | 풀샷 |
| 5~8 | ~5300×7950 | 0.667 | 387.0 | |

### 상품 그리드 템플릿 (섹션 `상품 프레임` 1:591)
| 템플릿 | 노드 | 이미지 셀 | 사용 |
|---|---|---|---|
| `products_7` | 1:421 | 122×90 | look1 (사용자 제작) |
| `products_6` | 1:51 | 130×124 | 미사용 |
| `products_5` | 1:159 | 130×124 | look2 |
| `products_4` | 1:219 | **130×204** | look3~8 |

- `products_4` 셀 좌표: 이미지 (45,30)/(205,30)/(45,305)/(205,305) · 라벨 (45,244)/(205,244)/(45,519)/(205,519) · 아이콘 = 라벨 x+106
- **`products_3` 템플릿 없음** → look7(3상품)은 products_4 복제 후 우하 셀 3개 노드(Frame 9·Frame 11·Group 1660) 제거 + 하단 셀을 x=125(라벨 125·아이콘 231)로 **가로 중앙 정렬**

### 면적 정규화
`w = √(S·a)`, `h = √(S/a)` (a = alpha bbox 비율), 셀 초과 시 비율 유지 클램프.
| 페이지 | S | 근거 |
|---|---|---|
| `02-swp` (products_5) | **7,473** | 셀 130×124, SOCKS(a=0.486)가 세로 124 꽉 참 |
| `03~08-swp` (products_4) | **13,500** | 셀 130×204. 사용자 제작 `01-swp` 실측 평균 면적 6,050 / 셀면적 11,790 = **fill 51%** 를 26,520에 환산 |

> `01-swp` 역산 근거: rect 크기 ÷ 610(원본 캔버스 폭) = scale → alpha bbox × scale = 실제 노출 면적. 7개 상품 4,988~6,724, 평균 6,050.

### 상품 배치 순서
- **PPT 슬라이드 순서 그대로**
- `products_5`/`products_7` (엇갈린 2열) = **열 우선** (좌열 위→아래, 그다음 우열)
- `products_4` (정렬된 2×2) = **행 우선** (좌상→우상→좌하→우하)

### 텍스트
- 폰트 **Montserrat** — 카테고리 SemiBold 12 / 품번 Medium 10
- 라벨 프레임(`Frame 5`)은 VERTICAL auto-layout · w=89 고정
- ⚠️ 13자 품번은 89px에서 2줄로 깨짐 → **품번 텍스트에 `layoutSizingHorizontal = "HUG"`** (`textAutoResize`만 바꾸면 부모 auto-layout의 FILL 때문에 안 먹음). HUG 후 84~102px, 아이콘(offset 106)과 최소 2px 간격 — 사용자가 그대로 두기로 결정.

### Figma MCP 메모
- `upload_assets` count=N → `multipart/form-data` `file` 필드로 병렬 POST → 응답에 `imageHash` + `placedOnNodeId`
- 임시 노드가 페이지에 생성되므로 **작업 후 `placedOnNodeId` 전부 `remove()`**
- 파일 매칭은 응답의 `sizeBytes` 로 (순서 보장 안 됨)
- 한 번 올린 `imageHash` 는 같은 파일 내 다른 노드에 재사용 가능

---

## C. CdBd 상품 링크 연결 (editor 5721)

### 대상 구조
`editor.pages` 8페이지 × 2블록 (둘 다 `type: "image"`)
- `blocks[0]` = 화보 (layers 없음)
- `blocks[1]` = 상품 그리드 + **`layers[]`** ← 여기에 링크
- 레이어 `filename` = 카테고리 소문자 (`cap`, `sweater`, `pants`, `shoes`, `outer`, `inner`, `vest`, `culotte`, `socks`, `bag`, `gloves`, `golf shirt`) + `gif_{N}` 1개
- 레이어는 이미 사용자가 생성·배치 완료. **`linkButton.link.href` 만 비어 있던 상태**

### 매핑 규칙
- 페이지 N = look N (레이어 이름 집합으로 교차 확인)
- URL = `https://www.kolonmall.com/Brands/WAAC/Product/{품번}` (PPT slide N)
- **동명 레이어 구분은 `position.x`** — P1만 `pants`·`shoes` 가 2개씩:
  `x≈37` = 좌열(MAN) / `x≈81` = 우열(WOMAN)
- 다른 페이지 패턴: 좌열 `x≈38` / 우열 `x≈80`
- `gif_N` 레이어는 링크 없음 (스와이프 안내)

### 적용 형식 (사용자가 UI로 넣은 P1 `cap` 과 동일)
```json
"linkButton": {
  "type": "url",
  "link": { "href": "https://www.kolonmall.com/Brands/WAAC/Product/…", "openNewTab": true },
  "data": {}, "phone": {}
}
```
→ **`linkButton` 키만** 변경 (디자인 보드 「링크 연결」 1:1). 위치·크기·이미지 미변경.

### 결과 (35개)
| P | 레이어 | P | 레이어 |
|---|---|---|---|
| 1 (7) | cap·sweater·pants·shoes(좌) + outer·pants·shoes(우) | 5 (4) | cap·golf shirt·inner·pants |
| 2 (5) | sweater·inner·pants·bag·socks | 6 (4) | cap·sweater·inner·pants |
| 3 (4) | vest·culotte·socks·shoes | 7 (3) | cap·golf shirt·pants |
| 4 (4) | cap·outer·pants·gloves | 8 (4) | outer·inner·pants·gloves |

- 드라이런에서 P1 `cap` 이 **사용자 수동 입력값과 완전 일치(`유지`)** → 매핑 로직 교차 검증됨
- **이미 게시된 상태**라 `editor` + `editor_published` **둘 다 PATCH** (각 204)
- 검증: 양 테이블 35/35 · `get_editor_status_v2` → `is_available: true` · 게시 페이지 HTTP 200(270KB) SSR HTML에 품번 31종 전부 노출
- 스크립트: `/tmp/waac_links.py` (`--apply` 없으면 드라이런)

---

## D. 인증 세팅 (이 머신에 신규 구성)

기존엔 `~/.config/cdbd/credentials.json` 에 openai 키만 있고 CdBd 헬퍼가 없었음 → **`auth.py` 신규 작성**.

- Supabase: `https://jzwrixxevnfogfjzgaqz.supabase.co`
- anon key: `https://www.cdbd.in/login` HTML → `_next/static/chunks/*.js` 에서 `eyJhbGci…`(payload `role: anon`) grep
- 최초 1회만 **password grant**로 토큰 발급 → `refresh_token` 만 저장, **비밀번호는 저장하지 않음**
- 이후 `~/.config/cdbd/auth.py` 의 `get_credentials()` 가 refresh_token으로 access_token 자동 발급 + 회전 저장 (mode 600)

```python
import sys, os; sys.path.insert(0, os.path.expanduser('~/.config/cdbd'))
from auth import get_credentials
c = get_credentials()   # access_token, user_id, email, supabase_url, supabase_anon
```
- 진단: `python3 ~/.config/cdbd/auth.py`
- 게시 URL 조립: `user_url.site_key` + `editor_url.url_key` → `https://www.cdbd.in/{site_key}/{url_key}`
  (이 계정 site_key = `mustard73`. 문서에 적힌 `2account` 는 `account@emka.group` 전용)

---

## E. 미완 / 사용자 확인 대기

1. **룩3 SOCKS(`WGLCX26141WHX`) 누끼 리소스 없음**
   `리소스/3/WWKCA26603NYX_LM1 (1).webp` 는 md5까지 동일한 **완전 중복 파일**. 양말 이미지 자체가 없음.
   → `03-swp` 좌하 셀은 `#d3d3d3` 플레이스홀더 유지 + 텍스트만 입력. 셀 이름 `WGLCX26141WHX (누끼 없음)`.
   **CdBd 링크는 이미 연결돼 있음** → 이미지만 받으면 Figma 채우고 CdBd 그리드 이미지 재업로드 필요.
2. **룩3 SHOES 면적 미달** — `WI4GX26101BKX` 는 526×246(비율 2.14)로 납작해 셀 폭 130에 먼저 걸림 → 면적 7,904 (다른 상품 13,500). 좌우 clip 허용 여부 미결.
3. **룩7 레이아웃** — products_3 템플릿 부재로 임의 구성(우하 제거 + 하단 중앙). 승인 대기.
4. **CdBd P7 `cap` 레이어 위치 이상** — `position {x:100, y:65}`. 다른 페이지 패턴(좌 38 / 우 80)에서 벗어남 → 화면상 CAP 상품 위에 안 얹혀 있을 가능성. 링크는 정상.
5. **룩4 GLOVES 코드 불일치** — PPT 텍스트 `WGHCX26110WHX` vs 링크/리소스 `WGHCX26210PIX`. **사용자 지시로 링크 기준 채택**.
6. 중복 상품: `WMWAA26606WIX`·`WMPNA26620BEX`·`WGRCX26612WIX`(look1&6), `WMPNA26620NYX`(look5&7) — 정상.

---

## F. 이 프로젝트에서 새로 확인된 규칙

- **단색 배경 리소스의 누끼는 Figma AI 요청 불필요** — 테두리 플러드필 + alpha bbox 크롭으로 충분(ML 아님). 전역 색상 임계값은 흰 상품을 파먹으므로 반드시 **플러드필**.
- **Figma auto-layout 자식 텍스트의 hug는 `textAutoResize` 가 아니라 `layoutSizingHorizontal = "HUG"`** — 부모가 FIXED 폭이어도 자식은 부모 폭을 넘어설 수 있음.
- **면적 정규화 S는 "기존 제작 페이지 실측 → fill 비율 환산"으로 잡으면 페이지 간 밀도가 일관됨** (셀 크기가 템플릿마다 달라도 대응 가능).
- **게시된 에디터의 데이터 수정은 `editor` + `editor_published` 둘 다 PATCH** (published가 별도 snapshot).
- **CdBd 게시 URL은 계정별 `user_url.site_key` 를 조회해서 조립할 것** — `2account` 하드코딩 금지.

---

## 관련 문서
- [[1-4. CdBd 콘텐츠]] — 레이어 `block.layers[]` schema · `linkButton` 규칙 · Supabase 직접 자동화
- [[2-4. 화보·상품]] — 누끼 alpha bbox 크롭 · 면적 정규화 공식
- [[2-1. 공통]] — 화보 = 별도 자식 RECTANGLE · 비크롭 원칙
- [[컨텍스트: 이선호/2026-06-11 swp 정의·검증 통합 + Kubota 시안·CdBd 데이터관리.md]] — 메이크뷰 → CdBd 이관 선례(Kubota)
