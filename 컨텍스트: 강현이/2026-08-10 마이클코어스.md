# 2026-08-10 마이클 코어스 CdBd 03~06 페이지 구현

## 요약
마이클 코어스 CdBd 에디터 **5400**(title "MICHAEL KORS", type multi, pageSize 380×580)에 Figma 03·04·05·06 페이지를 구현.
방식 = **에디터의 2페이지를 그대로 복제(구조·스타일·페이지 배경 동일) → 콘텐츠만 교체**.
결과: 총 6페이지 (기존 커버+02 템플릿 2P + 신규 03~06 4P). **에디터(초안)에만 반영, 게시 안 함**.

- 에디터: https://www.cdbd.in/editor/5400
- Figma: https://www.figma.com/design/FH9l7G3lRbEQz4X49rIvII/마이클코어스_2608?node-id=5-255 (fileKey `FH9l7G3lRbEQz4X49rIvII`)
- CdBd 계정: account@emka.group / user_id `523daa95-b43d-4a13-9919-e5ce7c097e06` (이비엠과 동일)
- 소스: `/Users/mustard/Desktop/mustard/CDBD/8월/2608_마이클코어스/`

## 페이지 ↔ 상품 ↔ URL 매핑
엑셀 `문서/Fall Coupon_제품리스트.xlsx` Sheet1 (R3=상품명, R4=설명, R5=URL, C2~C6 = 페이지 02~06)

| Figma노드 | 페이지 | 상품 | 에셋코드 | [상품 자세히 보기] 링크 |
|---|---|---|---|---|
| 15:8 (02-1=15:12) | 3 | 레일라 레오파드 크로스바디 | 3.32F6A9IC0H | ❌ **TBD → 제외**(지시) |
| 15:20 (15:24) | 4 | 롱슬리브 미니 원피스 | 4.MF683OHLE9 | ✓ pno=543817ED…(search_str MF683OHLE93031) |
| 15:32 (15:36) | 5 | 브라이언트 스몰 체인 숄더 | 5.30F6AYTL5S | ✓ pno=4963E568…(30F6AYTL5S338) |
| 15:44 (15:48) | 6 | 브라이언트 레더 로퍼 | 6.40F6BYFP2L | ✓ pno=8C67C2AC…(40F6BYFP1L201) |
| 5:287 | (기존2P) | 해밀턴 모던 라지 플랩(02) | 2.30F6ANXS7E | TBD(범위 밖) |

- 사용자 지시: "02·03 페이지는 엑셀 URL 정보 없음(TBD) → 그거 제외하고 작업". 02는 애초에 구현 대상 아님, 03은 구현하되 버튼 링크만 제외.
- 전체 URL은 엑셀 R5C4~C6 참조.

## 에디터 2페이지(템플릿) 구조 = 단일 제품 상세
`editor.pages[1]` (order 2, id `cb7n4zOMVikx93kW4FlVg`), background `rgba(237,250,255,1)`(연파랑), 7블록:
- block0 text "2026 FALL COLLECTION" — **disabled(꺼짐)**
- block1 image = MICHAEL KORS 로고 (360×38, width 45%) — 전 페이지 공통(URL 재사용)
- block2 image = 제품 (width100%, padding `10px 50px 0`)
- block3 text = 상품명 (bold 19px, notoSansKr, center, lineHeight 1.5)
- block4 text = 설명 (14px, color rgba(41,41,41,0.9), 3줄 linebreak, lineHeight 1.3)
- block5 **button 카드 = disabled(꺼짐)** (innerStyle dark, buttonStyle 40%) — 미사용
- block6 text = **다크배경(rgba(17,17,17)) "상품 자세히 보기" = 실제 보이는 버튼** (margin `27px 115px 0`, 13px, 흰색)

⚠️ 실제 "상품 자세히 보기" 버튼은 button 카드가 아니라 **다크배경 텍스트 카드(block6)**. button 카드(block5)는 꺼져 있음.

## 핵심 실측/기법 (재현용)
- **제품 이미지 = 02-1 프레임(255×272) 2x export**. Figma 프레임은 배경이 페이지 연파랑(237,250,255)으로 채워져 나옴 → **PIL로 그 파란색(±12)만 투명 처리**해 510×544 투명 누끼 생성(현재 해밀턴 이미지 `510×544 RGBA`와 동일 규격, 코너 alpha=0). 흰 배경 프리뷰로 파란 헤일로 없음 확인. 원본 누끼는 각 rect(16:59 등) 안에 이미 배경제거+포지셔닝 되어 있음(제품별 배치/크기 다름·밴드 중앙정렬).
- **텍스트 카드 "링크 연결" = lexical `link` 노드**(NOT `linkButton`, NOT `link` 필드). UI로 검증함:
  - 구조: custom-paragraph.children = `[{type:"link", rel:"noreferrer", target:"_blank", title:null, url:URL, children:[textnode]}]`
  - ⚠️ UI로 링크 걸면 text format이 0→8(**밑줄**)로 바뀜 → 원본 디자인엔 밑줄 없음. **format 0 유지 + link 노드만** 넣으면 밑줄 없이 링크 동작(직접 PATCH로 렌더 검증 완료). 링크는 링크 연결 UI 필드에도 그대로 노출돼 사용자 편집 가능.
- **상품명**: 신규 4종 모두 1줄(Figma 그대로) → paragraph 1개. **설명**: 3줄 linebreak, Figma 줄나눔에 맞춰 단어경계로 분할(03·05는 Figma와 동일, 04·06은 단어경계 버전).
- 신규 페이지 = template deep-copy, page id = nanoid(21), block id = uuid4 재발급, order 3~6.

## 인증 (이 머신 = jeonjuyun / HOME=/Users/mustard)
- CdBd 자동화 헬퍼 **없음**(`~/.config/cdbd/`엔 openai 키만). 이비엠과 동일 상황 → 인라인 처리.
- **GStack 브라우저 headed 로그인**으로 토큰 추출:
  1. `B=/Users/mustard/.claude/skills/gstack/browse/dist/browse; $B connect`(headed) → `$B goto cdbd.in/login` → `$B focus` → 사용자가 account@emka.group 로그인
  2. `$B state save cdbd` → **`.gstack/browse-states/cdbd.json`(평문·21쿠키)** 저장. 세션 끊기면 `$B state load cdbd`로 복원.
  3. 토큰: state 파일의 `sb-jzwrixxevnfogfjzgaqz-auth-token.0/.1` 쿠키 이어붙여 `base64-` 제거 후 b64decode → JSON에서 access_token/refresh_token/user 추출.
- **Supabase**: `https://jzwrixxevnfogfjzgaqz.supabase.co`, anon key는 로그인 후 JS 번들에서 grep(`eyJhbGci…` payload에 `anon`). REST: `apikey`+`Authorization: Bearer <access_token>`.
- ⚠️ `$B cookies`는 값 마스킹됨 → **state 파일에서 추출**해야 함.

## 자동화 시퀀스 (실행 내역)
1. 이미지 라이브러리 = **`image_modal` id 1201** (user별 1행, images[] 배열, 작업 시점 1162장→1166장). 엔트리 스키마: `{id(uuid), url(전체 signed URL), name, path, size, type, pexels:null, user_id, folder_id:null, created_at, deleted_at:null, is_deleted:false, resolution:"W*H", is_bookmarked:false}`.
2. 이미지 업로드 3-call(인라인): ① `POST storage/v1/object/user_image/{uid}/{YYYYMMDDHHMMSS}_{suffix}.png`(x-upsert:true) → ② `POST storage/v1/object/sign/user_image/{path}` body `{expiresIn:946080000}`(30년) → signedURL 앞에 `SUPA+"/storage/v1"` 붙임 → ③ `image_modal.images` PATCH(prepend). 4장 이름 = 상품명.
3. 페이지 조립 후 `PATCH /rest/v1/editor?id=eq.5400` body `{pages:[...6페이지...]}` (204). **PATCH 전 브라우저를 editor 밖(/home)으로 이동**(autosave race 방지).
4. 검증: 재GET 6페이지 + 에디터 재오픈 썸네일 스샷(6페이지 정상 렌더 확인). page2 원본 복원(테스트 링크 제거·밑줄 없음) 확인.

## 미완 / 사용자 확인 대기
1. **게시 안 함** — 에디터엔 이미 게시본 존재(`editor_published.url_key = ekmpdg`). 현 작업은 **초안(editor)에만** 반영 → 실제 URL 미반영. 게시 여부 사용자 확인 필요(게시 직전 확인 원칙).
2. **"2026 FALL COLLECTION" 문구** — Figma 03~06엔 로고 위 소제목 있으나, 복제한 2페이지에서 해당 텍스트 카드가 disabled → 신규도 꺼짐. Figma대로 켤지 확인 대기.
3. 기존 2페이지 = 이미지 해밀턴(브라운백) + 상품명 "레일라 엑스트라 스몰 레더 크로스바디" 불일치(범위 밖, 미변경). 정리 요청 시 대응.

## 참고 규칙 (이번 확인)
- CdBd 이미지 = 반드시 이미지 라이브러리(image_modal) 경유 (raw storage 직박 금지). 이 머신엔 image_library.py 없어 인라인 구현.
- 텍스트 카드 링크 = lexical link 노드(format 0로 밑줄 제거). button 카드의 linkButton과는 다름.
- 이 사용자는 에디터 탭 항상 닫아둠 → close 확인 프롬프트 생략, 단 우리 브라우저는 PATCH 전 editor 밖으로.
- 임시 토큰은 `/tmp/mk_cdbd/`에 뒀고 at/rt는 작업 후 삭제. anon/uid는 남음.
