# 2026-08-03 이비엠(E.B.M) CdBd 등록 — 이미지만 선등록

## 요약
E.B.M 2026 SUMMER 모바일 카탈로그(6P VER)를 CdBd 에디터 5372에 등록.
상품코드·버튼·링크는 8/10 전달 예정이라 **이번엔 이미지만** 넣음. 게시(URL 생성) 안 함.

- 에디터: https://www.cdbd.in/editor/5372 (title "E.B.M ", type multi)
- pageSize: **380×618** (이미지 비율 760×1236과 정확히 일치)
- CdBd 계정: account@emka.group / user_id `523daa95-b43d-4a13-9919-e5ce7c097e06`

## 소스 파일
`/Users/mustard/Desktop/mustard/CDBD/7월/2607_이비엠/`
- `작업파일/02~06.jpg` (760×1236) — 완성 페이지 이미지
- `작업파일/og.png` (1600×800), `리소스/이미지,영상/kakao_preview.jpg` (800×400)
- `리소스/이미지,영상/1p_ebm_AUG_resize.mp4` — 1페이지 영상 (114MB)
- 문서: `문서/260810_EBM_8월_모바일카탈로그의뢰_1차.pptx`

## PPT 의뢰서 요구사항 (6P VER)
| 페이지 | 파일 | 내용 | 나중에 (8/10) |
|---|---|---|---|
| 1p | mp4 | 오프닝 영상 | — |
| 2p | 02.jpg | 표지 (elsewhere becoming me) | 상품코드 + 버튼 (하단 로고 X) |
| 3p | 03.jpg | PROMOTION 10% 쿠폰 | 쿠폰에 기획전 링크 (상단 상품코드 X) |
| 4p | 04.jpg | 화보 (레더재킷) | 상품코드 + 버튼 |
| 5p | 05.jpg | 화보 (스커트) | — |
| 6p | 06.jpg | 화보 + 공식몰/INSTAGRAM 버튼 | 공식몰 링크·인스타(instagram.com/e.b.m_official)·로고/상품코드(블랙)·사운드 버튼 |

## 한 작업
1. **인증**: 이 머신엔 CdBd 자동화 인증(Supabase refresh_token·figma_pat)·헬퍼(auth.py/image_library.py) 없음. `~/.config/cdbd/credentials.json`엔 openai 키만. → GStack 브라우저 headed(`$B connect`)로 띄워 account@emka.group 직접 로그인 → 쿠키에서 refresh_token/access_token/user_id 추출해 사용 (임시 `/tmp/ebm_cdbd/at.txt`, `rt.txt`).
   - refresh_token: `7bjkrlouxkde` (rotation 주의, 만료 가능)
2. **에디터 기존 상태**: 2페이지 존재 — page1(order1)=기본 placeholder `default-img-multi.gif`(영상 자리), page2(order2)=02.jpg 표지 이미 UI 업로드됨.
3. **이미지 라이브러리 등록**: 03·04·05·06.jpg 4장을 image_modal(modal_id 1201, 기존 1078장) 3-call(storage POST→sign 30년→image_modal PATCH prepend)로 정식 등록. suffix mixed-case로 발급.
4. **페이지 추가**: 03~06을 order 3~6으로 append, `editor.pages` PATCH. 카드 스타일은 기존 표지 블록 복제(image, 비율=원본, 크기=100, 정렬=중앙, padding 0 풀블리드). → 총 6페이지.
5. **검증**: DB 재GET 6페이지 확인 + 에디터 스크린샷으로 썸네일 3~6 정상 확인. race condition 없음(재GET 유지). PATCH 후 브라우저는 editor 밖(/home)으로 이동.
6. **게시 안 함** (요청대로 이미지만).

## 미완 (8/10 링크·코드 도착 후)
- 2p·4p 상품코드+버튼, 3p 쿠폰 기획전 링크, 6p 공식몰/인스타/로고·상품코드(블랙)/사운드버튼
- 1p 영상, OG(og.png)·카톡썸네일, URL 생성/게시

## 1페이지 영상 이슈 (사용자가 직접 코드 카드로 넣음)
사용자가 page1에 「코드」카드 + Vimeo iframe 임베드(video 1215081617, autoplay+muted+loop)를 넣음. 상단에 "음소거 해제"(Unmute) pill이 뜸.
- **원인**: "음소거+자동재생" 조합일 때 Vimeo가 강제로 띄우는 오버레이. cross-origin iframe이라 CSS로 못 지움. controls 파라미터로도 안 없어짐(A: 기본, B: controls=1 둘 다 pill 유지 — 브라우저 실측 확인).
- **선택지**:
  - 자동재생 유지 → pill 남음 (현 상태)
  - `background=1` → pill+컨트롤 전부 제거 (무음 루프, 하단 진행바·음량도 사라짐)
  - 자동재생 빼기(autoplay=0) → pill 없음 + 하단 컨트롤바 유지, 대신 첫 화면 재생버튼 뜨고 탭해야 시작
- 사용자 요구: "하단 진행바·음량은 있어야 함" → background=1 불가. 결론 미정("일단 알았어")로 보류.

## 참고 규칙 (이번에 확인)
- CdBd 이미지 = 반드시 image_modal(이미지 라이브러리) 경유 (raw storage POST 금지). 이 머신엔 image_library.py 헬퍼 없어 인라인 구현.
- 에디터 열린 상태 PATCH = autosave race. 이 사용자는 항상 에디터 닫아둠(2026-07-14 지시) → close 확인 생략 가능. PATCH 전 gstack 브라우저는 editor 밖으로.
- 이 머신 사용자/경로: jeonjuyun / HOME=/Users/mustard (문서 CLAUDE.md의 leesunho 경로와 다름 — 헬퍼·credentials 재설정 필요).
