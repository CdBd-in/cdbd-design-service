# 세션 컨텍스트 — 2026-06-24 대아청과 소식지 시안 (신규 카테고리) · 표지 채소 유통지도 · 지원현황/리뷰 정리 · 시안 표지 · 저장물량 표 전치

> 이 문서는 새 대화에서 작업을 바로 이어갈 수 있도록 작성된 컨텍스트 파일이다.
> 이전 세션: [[2026-06-11 swp 정의·검증 통합 + Kubota 시안·CdBd 데이터관리]] (룩북 계열)
> ⚠️ **소식지는 룩북이 아닌 신규 카테고리** — 이 문서가 소식지 첫 컨텍스트 기록이다. (이 세션 = 소식지 시안 빌드 후반부 refinement 모음)

---

## 0. 프로젝트 개요 (소식지 = 신규 카테고리)

- **대아청과 소식지** = 기존 「메이크뷰(통이미지 스크롤)」로 만들던 사보를 이번에 **CdBd 멀티페이지(카드 조합)**로 제작하는 **신규 카테고리**. 룩북과 별개.
- 이번 1호가 카테고리 **기준/템플릿**을 세운다. **시안만** 제작 (초안 X), **한글 원고 1~3p 내용**, **2타입(A·B)**.
- **규칙 문서**: `소식지/` 폴더
  - `소식지/1. 제작 프로세스.md` — 4단계 워크플로우(콘텐츠기획→스켈레톤→시안 A/B→CdBd), 스켈레톤 규칙
  - `소식지/2. 디자인 가이드/2-1. 공통.md` — 표지 예외·유통지도·헤더/푸터·섹션 타이틀 분리·시안 표지·CdBd 제약·페이지별 디테일
  - `소식지/2. 디자인 가이드/2-2. 색상 팔레트.md` — 역할매핑·5색 팔레트(A/B)·의미색
- **원고**: `~/Downloads/대아청과/문서/2026_ _  소식지 원고.hwp` (olefile+zlib raw deflate로 추출). 리소스: `~/Downloads/대아청과/리소스/`(CI·서명·행사사진), 작업파일 폴더는 비어있음.
- **Figma 파일**: `YjFunCmtR5nVaF6XiApqxn` (대아청과_2606) · 페이지 `모바일` (node `1:2`)

### 페이지 그리드 (⚠️ 위치 절대 이동 금지 — pitch 440 · y=2519)

현재 폴리시된 시안(이 세션 작업 대상):

| 안 | 표지 | 인사말 | 지원현황 | 리뷰·전망 |
|----|------|--------|----------|-----------|
| **A** (에디토리얼) | `2015:2` (x-1068) | `2057:2` (-628) | `2055:2` (-188) | `2068:2` (252) |
| **B** (모던) | `2017:2` (x832) | `2054:2` (1272) | `2051:2` (1712) | `2067:2` (2152) |

- 전부 y=2519, pitch 440 (380폭+60갭). 좌측에 **이전 버전 시안** `2011:*`(구 A/B)·스켈레톤 `2004:*` 존재(건드리지 말 것).
- **src 보드** `2012:45`: CI 로고 `2012:44`(심볼 `2012:4`+워드마크 `2012:5`, 잉크가 bbox 꽉 채움 padding 0), 대표이사 서명 `2012:2`, 표지 배경 지도 `2047:6`(1024×1536), 품목 일러스트 **배추 `2047:2` · 양배추 `2047:3` · 무 `2047:4` · 대파 `2047:5`**(각 292×292, 밝은 그린/크림).

---

## 1. 사용자 요청 (이 세션, 순서대로)

1. (요청 묶음 A) **프레임 위치 이동 금지** + ① 표지 = 한글 원고대로 **채소 유통지도**(지역별 특산품 → 서울 대아청과로 수렴, 연결선·대아청과 별도 레이어) ② 지원현황: 상·하단 중복된 「2026년 6월 기준」 → 상단 텍스트를 하단으로 ③ 지원현황: 각 「기준」 앞 `*` + 위 텍스트와 더 큰 명도 차이 ④ 리뷰전망: 리뷰/전망 섹션 분리
2. **시안 표지 제작해줘**
3. **시안 표지 규칙 다시 확인** → 불필요한 텍스트 삭제 + 로고 면적 수정 + **텍스트에 임의 색상 넣지 말 것**
4. (저장물량) **이 테이블 행/열 바꿔줘**(node `2072:162`) — 첨부 이미지처럼
5. (본 문서) 세션 컨텍스트를 이선호 폴더에 기록

### 결과 (한 줄)
표지 2종을 「채소 유통지도」(지도/연결선/품목마커/대아청과 4레이어)로 재구성, 지원현황·리뷰 텍스트 위계 정리, **시안 표지**(룩북 규칙대로 로고+TYPE만·로고 2.5% REST 실측 검증), 저장물량 표를 **연도=행** 미니멀 B&W로 전치. 모든 페이지 프레임 위치 보존.

---

## 2. 표지 = 채소 유통지도 (A·B, node `2070:3` / `2073:2`)

### 원고 컨셉
"전국 농산물이 모이는 곳" → 한국 지도 위 **지역별 품목 배지**에서 **서울 「대아청과」 허브 핀**으로 **점선 연결선**이 수렴.
- **지역–품목 매핑(원고)**: 제주=무·양배추(·브로콜리) / 전라=대파(·쪽파) / 충청=대파(·옥수수) / 강원=배추(·옥수수) → 서울. **보유 일러스트 4종으로 대표**(브로콜리·쪽파·옥수수는 소스 없음).

### 빌드 핵심
- 기존 표지의 작은 지도+캐릭터 행 삭제 → 컨테이너 「유통지도」에 지도(`2047:6` 클론, 221×332) + 3개 그룹.
- **레이어 4분할(별도 레이어)**: `지도` / `연결선` / `품목 마커` / `대아청과`.
- **배지** = 어두운 포인트색 원 + 연한 품목 일러스트(밝은 일러스트가 밝은 지도에 묻히는 것 방지). **허브 핀** = 강조색 원 + 화이트 닷 + 「대아청과」 라벨.
- **연결선** = `createLine` + `relativeTransform=[[cos,-sin,P1x],[sin,cos,P1y]]` (회전 부호 모호성 회피 — dx,dy에서 직접 cos/sin 산출), dashPattern [5,4].
- 타입색: **A** 딥그린 배지·오커 점선·앰버 허브 / **B** 틸 배지·틸 점선·파인 허브.
- 지역 좌표(표지 로컬, 지도 x79.5·y168·221×332 기준): 서울/허브 (158,252)·강원 (238,250)·충청 (168,322)·전라 (152,392)·제주 무 (120,476)·제주 양배추 (156,484).
- 문서화: `2-1. 공통.md` 「표지 일러스트 = 채소 유통지도」 신설.

---

## 3. 지원현황 정리 (A `2055:2` / B `2051:2`)

- **상·하단 중복 「2026년 6월 기준」 해소**: 상단 메타 프레임(A `2055:11`+divider `2055:13` / B `2051:11`) **삭제** → 하단 각주를 `※ 2026년 6월 기준 · 영업지원팀`으로 통합(영업지원팀 정보 흡수).
- **각 「기준」 줄**: `기준 · …` → **`* 기준 · …`**(앞에 `* `), fill opacity **0.52→0.40**으로 더 흐리게(본문 0.72 대비 명도차 확대). A 기준노드 `2055:21/30/39/48`, B `2051:17/22/27/32`.
- 두 페이지 모두 auto-layout(AUTO)이라 삭제 시 자동 reflow(높이만 변동, 위치 유지).
- 문서화: `2-1. 공통.md` 「페이지별 디테일」.

---

## 4. 리뷰·전망 섹션 분리 (A `2068:2` / B `2067:2`)

- 한 품목 안에서 「하반기 리뷰」와 「2026 상반기 전망」이 라벨만 있고 한 블록 → **사이에 구분선 삽입**(CdBd 「구분선·여백 카드」 = 공인된 섹션 분리 도구).
- 구현: 각 품목 content frame에서 `characters.includes('전망')`인 전망 라벨 index 앞에 `insertChild`로 divider wrapper(상하 padding + FILL 라인) 삽입.
  - A content frames `2068:15/28/41/54` → divider `2079:2/4/6/8` (딥그린 0.18, 내지 위 얇은 라인).
  - B content frames(틴트 패널) `2067:13/22/31/40` → divider `2081:2/4/6/8` (잉크 0.14, 패널 안 얇은 라인).
- 문서화: `2-1. 공통.md` 「페이지별 디테일」 — 증가=블루▲/감소=레드▼는 각각 단일색 텍스트 카드.

---

## 5. 프레임 위치 검증·복원 (⚠️ 최우선 제약)

- 표지 재빌드 후 점검: A 표지가 `-1074`로 6px 드리프트 → 그리드값 `-1068`(=인사말 -628−440)로 **복원**. y=2519.
- B 표지 시작값(1057/2528)은 **stale read**였고 실제 그리드값 832/2519가 정위치(인사말 1272−440)임을 확인 → 그대로 둠.
- 지원현황·리뷰는 위치 불변(내용 편집은 높이만 변동). **모든 top-level 프레임은 페이지 직속·layoutMode NONE** → x/y 직접 set이 유일 정상 경로.
- 교훈: MCP get_metadata/screenshot에 **stale read**가 종종 발생(표지 bg 색·좌표). 의심되면 use_figma로 실측 재확인.

---

## 6. 시안 표지 (제안용 맨 앞 · board `2086:2`)

- **룩북 「1-2. 시안」 ‘시안 표지’ 규칙**을 소식지에 적용: A안·B안 구분 맨 앞 표지(콘텐츠 표지 ≠ 시안 표지). 룩북 원본 = **로고만 교체**.
- **구성 = 흰 배경 + 대아청과 CI(중앙) + 「TYPE - A / TYPE - B」 외곽선 박스, 이 둘 뿐.**
  - covers `2086:3`(TYPE-A) / `2086:28`(TYPE-B), 회색 보드 `#ECECEC`에 나란히. 위치 x-1068·y1879(현 시안 행 바로 위 빈 공간). CdBd 등록 시 각 안 1페이지(380×580)로.
- ⚠️ **요청 3에서 정정**(처음엔 에브로우·무드라벨·포인트색 글자 추가 → 규칙 위반):
  - **불필요한 텍스트 삭제**: 에브로우 `2026 상반기 소식지`·무드 `에디토리얼`/`모던` 제거.
  - **텍스트 임의 색상 금지**: `A·B` 글자 딥그린/틸 → **중립 다크 `#292929`**, `TYPE -` 회색, 박스 테두리 회색.
  - **로고 면적 2.5% 검증**: Figma **REST API export**(PAT는 `~/.config/cdbd/credentials.json` `figma_pat`)로 CI 알파 bbox 실측 → **잉크가 bbox 100% 채움(padding 0)** → 폭 178 = 정확히 2.5%(≈5,510px²). 그대로 둠.
- 문서화: `2-1. 공통.md` 「시안 표지」 신설(미니멀·임의 텍스트/색 금지·로고 2.5%).
- 💡 **base64 export 함정**: `use_figma exportAsync`+`figma.base64Encode` 반환 b64는 tool output 20kb에서 truncation + 수동 paste 시 글자 누락(padding 오류). → **이미지 실측은 Figma REST API `/v1/images`로 URL 받아 다운로드**가 안전.

---

## 7. 저장물량 표 전치 (standalone `2072:162`)

- 사용자가 캔버스에 복제해 둔 표(x692·y3339, A 오커 스타일, 연도=열). 첨부 이미지대로 **행/열 전치 + 미니멀 B&W**.
- **결과**: 헤더 `구분 | 저장배추(톤) | 저장무(톤)`(볼드·가운데, 아래 두꺼운 선) / 행 2023~2026년·전년대비 증감율(구분열 가운데) / **수치 우측정렬** / **−5.2% 빨강·+16% 검정**(첨부 이미지=감소만 빨강) / 상하 진한 선·행간 옅은 회색 선. 344×220, 위치 보존.
- 구현: 컨테이너 children 비우고 absolute 배치(텍스트 + 라인 rect), 컬럼폭 [108,118,118].
- ⚠️ **미완**: 페이지 내 리뷰 표 2개(**A `2068:66` 오커 헤더 / B `2067:49` 포레스트 헤더**)는 **아직 가로방향(연도=열)** — 사용자에게 "페이지 표도 전치할까요?(각 안 포인트색 유지)" 제안 후 대기.

---

## 8. 확정 팔레트 (5색, `2-2. 색상 팔레트.md`) — 헤더 ≠ 섹션 제목

| 역할 | **A 에디토리얼** | **B 모던** |
|------|------------------|-----------|
| 배경 | `#F2F7F5` | `#F8F5F2` |
| 본문 | `#00473E` 딥그린 | `#232323` 잉크 |
| 헤더·푸터 | `#00473E` 딥그린 | `#078080` 틸 |
| **섹션 제목 배경** | `#8F6014` 오커 (배지 `#D2A24A`) | `#1F6E3C` 포레스트 |
| 라벨/금액 | `#1B6E37` 리프그린 | `#0B5C3B` 파인그린 |
| 보조 강조/패널 | 하이라이트 `#FAAE2B` 앰버 | 패널 틴트 `#EAEFEC` |
| 의미색(공통·고정) | 증가 `#2156C0` / 감소 `#C0392B` | 동일 |

- 라이브러리(cdbd-templates 1-2)에서 선택, 소식지는 텍스트 위계 많아 **4~5도**. 의미색은 인라인 불가 → 증가/감소 각각 단일색 텍스트 카드.

---

## 9. 항상 지킬 제약 (이 프로젝트)

- **프레임 위치 이동 금지** (top-level 프레임 x/y 보존, 내용 편집은 높이만 변동).
- **CdBd 구현 제약**(`2-1` 「CdBd 구현 제약」): 카드=1콘텐츠=1배경(중첩 배경 뱃지 금지) · 텍스트카드=1속성1색 · 가로배치=2단카드 · **표=이미지카드** · 폰트 Bold/Regular만 · 줄간격 0.2스냅.
- 표지만 예외(통이미지). 헤더=2단카드[제목 좌/아이콘 우](좌아이콘+중앙텍스트 불가), 푸터=◀로고▶, 둘 다 상단/하단 고정.
- 누끼/로고 = 배경제거+여백0+원본비율. 배경 가공만 Figma AI(에디터 전용).

## 10. 다음 단계 후보 (미완)

1. **페이지 내 리뷰 표 2개 전치**(A `2068:66`/B `2067:49`) — 제안 후 대기.
2. 브로콜리·쪽파·옥수수 일러스트 추가 여부(현재 4종으로 지역 대표).
3. 시안 표지 로고를 시각적으로 더 키울지(현재 규칙상 2.5% 정위치).
4. 시안 확정 후 **CdBd 에디터 구현(4단계)** — 멀티페이지 380×580, 시안표지→표지→인사말→지원→리뷰, OG·제목.
