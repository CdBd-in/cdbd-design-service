# 작업 컨텍스트 — 2026-05-29

**작업자**: 이선호
**주제**: swp(스와이프) 시안 제작 + 03 화보·상품 규칙 문서 전면 재정비 + 참조 이미지 통일

> 새 대화에서 작업을 바로 이어갈 수 있도록 작성된 컨텍스트 파일.

---

## 오늘 한 일 요약

마쥬(Maje) LOOK4 리소스로 **03-swp 스와이프 시안(A·B)**을 제작하고, 이어서 [[2-4. 화보·상품]] 문서를 전면 재정비했다. basic 유형별 텍스트 속성을 **Figma 실측**으로 개별화하고, **03 참조 이미지 12종을 섹션 캡쳐로 통일**했다. 이미지 가공 규칙(배경=Figma AI / 리사이즈·위치=use_figma)을 확정하고, 문서 경로·용어를 정리했다.

---

## 1. swp 스와이프 시안 제작 (마쥬 LOOK4)

- 리소스: `~/Downloads/마쥬/` 상품 이미지 + 엑셀(페이지·상품 정보) + MAJE 로고
- **누끼 12종**: 처음 rembg `isnet-general-use`로 배경 제거 → **흰 의류(반다나 셔츠·스트라이프 스커트)가 옷까지 깎임** → **BiRefNet-general**로 재가공(`providers=["CPUExecutionProvider"]` 강제, CoreML 오류 회피).
- 에셋 업로드(화보 LOOK_18·누끼 12·로고) → **A안 clean BG**·**B안 blur BG** swp 시안 제작 → 스크린샷 검증.
- 페이지 분할: 사용자 확인 → **2장**. 반다나 셔츠 포함, 정렬은 **상하 중앙**.
- ⚠️ 이후 규칙 확정: **배경 가공은 외부 ML 대신 Figma AI** 사용(아래 「이미지 가공 규칙」).

## 2. [[2-4. 화보·상품]] 전면 재정비

- 위계·중복 점검 후 **전면 재구성**: intro → basic → 공통(누끼 가공) → swp → scl → 피그마 레퍼런스.
- scl **유형 2개 추가**: `full-grid`·`blur-grid` (Figma Grid 오토레이아웃 · 카드 간격 0 · `03-2-BG` 카드 배경).
- `03-basic-link` 유형 추가 (라인 마커 + 추후 gif 「+」 버튼, 선·텍스트 색=배경 명도, 그림자 옵션).
- 내보내기 규칙 유형별 정리([[1-1. 공통]]): basic=전체 프레임 / swp=`03-BG`+`03-swp1~` / scl=`03-1`·`03-2`·`03-logo`·`03-2-BG`, scl-row 구분선 미내보내기.

## 3. swp 유형명·구조 정리

- 유형명 **clean / full / blur**로 통일 (오기입했던 `frame-` 접두사 제거 — 섹션명 기준).
- 각 유형 **공통 규칙을 섹션 상단**으로 이동 (basic 공통·swp 공통; scl은 「메인 화보 마커」가 이미 상단).
- swp 가이드(와이어프레임 `16089:991`) 기준 검수 → 3유형 **swp2·swp3 카드의 누끼가 하단 정렬**돼 있던 것을 **상하 중앙**으로, 라벨은 하단 정렬로 수정(6장). blur는 내부 `Frame17`이 absolute라 AL 변환 없이 좌표만 재배치.

## 4. basic 유형별 텍스트 속성 개별화 (Figma 실측)

기존 공통 표를 해체하고 유형별로 개별 표기:

| 유형 | 구성·정렬 | 크기 | 색 | 행간 |
|---|---|---|---|---|
| **full** | 상품명+가격 세로 · 우측 | 11px | 상품명 `#141414` / **가격 `#4A4A4A`(회색)** | 140% |
| **frame·footer** | 카테고리+상품명 한 줄 / 가격 · 양끝 | 11px | 모두 `#141414` | 160% |
| **link** | 카테고리+품번 · 좌상단 | **12px** | **`#000000`** | 100%(자간 0) |

- `03-basic-footer` 상세 규칙 추가(화보 풀블리드 380×494 + 로고 146×18 중앙 + 하단 정보 바 340·하단정렬·양끝).

## 5. 03 참조 이미지 12종 섹션 캡쳐로 통일

- 각 Figma 섹션을 1500px로 재캡쳐해 `룩북/참조 이미지/ref-03-*.png`(+`ref-그리드-*`) 전부 교체.
- `ref-03-scl-grid`=frame-grid2(2열) / `ref-그리드-scl`=frame-grid3(3열) / `ref-그리드-swp`=카드 예시(`16002:522`).

## 6. 문서 경로·용어 정리

- 자산 폴더명 변경 반영: `룩북/자산/` → **`룩북/참조 이미지/`** ([[CLAUDE.md]]·컨텍스트 노트).
- swp 유형명 cross-ref 정리([[2-2. 표지]]: `03-swp-blur BG` → `03-swp-blur`).

---

## 이미지 가공 규칙 (확정)

- **배경 삭제·배경 늘림 = Figma AI(에디터 전용)**. 자동화 API(`use_figma`)로는 호출 불가(`createImage`/`getImageByHash`만 존재) → 에디터에서 적용한 결과로 진행.
- **리사이즈·위치·scaleMode·imageTransform·crop 등 그 외 가공 = `use_figma`** (기존 방식, AI 아님).
- ⚠️ 로컬 rembg/BiRefNet 등 **외부 ML은 쓰지 않는다**.

## 주요 Figma 노드 (파일 `SI36czRu3lBgkPzyrnPYkB`)

- **swp**: clean `16008:562`(가이드 `16089:966`) · full `16041:339`(`16089:1191`) · blur `16008:563`(`16100:1732`) · 카드 예시 `16002:522`(와이어프레임 `16089:991`)
- **basic**: full `16008:129` · frame `16008:92` · footer `16041:267` · link `16100:2588`
- **scl**: grid2 `16008:687` · grid3 `16041:376` · full-grid `16089:1151` · blur-grid `16089:1150` · row `16008:688`
- 전체 타입 라이브러리 보드 `16100:1759`

---

## 미완료 / 다음 작업 참고

- [ ] swp 시안 **내보내기**(`03-BG`·`03-swp1~`) 및 데스크탑 **브랜드(국문) 폴더** 저장 — 국문 브랜드명 사용자 확인 필요.
- [ ] `ref-그리드-scl`을 3열 섹션 대신 예전처럼 **카드 그리드 크롭**으로 둘지 사용자 확인 가능(요청 시 되돌림).
- CdBd 로그인 정보는 **미보유** (필요 시 사용자 제공).
